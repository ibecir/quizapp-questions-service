spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: question
  application:
    name: QUESTION-SERVICE

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: debug

